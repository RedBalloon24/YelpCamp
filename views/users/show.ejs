<%- include("../partials/header") %>

<div class="row justify-content-center mb-3">
    <div class="col-md-4">
        <div class="card">
            <h1 class="capitalize text-center"><%= user.username %></h1>
            <% if(!user.avatarURL){ %>
                <img src="https://images.unsplash.com/photo-1457449940276-e8deed18bfff?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1050&q=80" class="card-img-top" alt="user profile image"> 
            <% } else { %>
                <img src="<%= user.avatarURL %>" class="card-img-top" alt="user profile image"> 
            <% } %>
            <div class="card-body">
                <div class="card-title">
                    <% if(!user.avatarURL){ %>
                        <p>"This is where you tell us about yourself!! Make it creative and unique to you!! :-)"</p>
                    <% } else { %>
                        <p>"<%= user.description %>"</p>
                    <% } %>
                </div>
                <p class="text-center"><strong><%= user.username %>'s Posts:</strong></p>
                <ul class="card-text list-group list-group-flush text-center mb-3">
                    <% campgrounds.forEach((campground) => { %>
                        <li class="list-group-item"><a href="/campgrounds/<%= campground.id %>"><%= campground.name %></a></li>
                    <% }) %>  
                </ul>
                <div class="text-center">
                    <% if(currentUser && user._id.equals(currentUser._id) || currentUser && currentUser.isAdmin){ %>
                    <a href="/users/<%= user.id %>/edit" class="btn btn-primary mt-3">Edit Profile</a>
                <% } %>
                </div>
                
            </div>
        </div>
    </div>
</div>

<%- include("../partials/footer") %>
